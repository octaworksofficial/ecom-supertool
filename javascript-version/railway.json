{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && npx prisma migrate deploy && npm run seed && npm run build"
  },
  "deploy": {
    "numReplicas": 1,
    "sleepThreshold": "10m0s",
    "restartPolicyType": "always"
  }
}
